function rollDice(count){const rolls=[];for(let i=0;i<count;i++){rolls.push(Math.floor(Math.random()*6)+1)}return{rolls,total:rolls.reduce((a,b)=>a+b,0)}}function renderRoll(target,label,dice){const result=rollDice(dice);const faces=result.rolls.map(r=>`<span class="die-face">${r}</span>`).join("");target.innerHTML=`<strong>${label}</strong><div class="dice-row">${faces}</div><div>Total: <span class="die">${result.total}</span></div>`}
